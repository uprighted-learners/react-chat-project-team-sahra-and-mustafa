//! holds our express call in router variable
const router = require("express").Router();
//! Message variable that holds our Message model
const Message = require("../models/message_model");

module.exports = router;
